[ cat: copy input to output (folklore one-liner). Run it with --eof zero;
  under the default EOF convention (cell unchanged) it echoes the last
  byte forever once input runs out, which is its own kind of lesson.
  Usage: echo hello | lake exe brainfuck --eof zero Langlib/Examples/Brainfuck/cat.b ]

,[.,]
