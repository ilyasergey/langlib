// cat: copies input bytes to output until end of input.
// readByte() yields -1 at EOF, so no EOF-convention drama here.
// Usage: echo meow | lake exe turpentine run Langlib/Examples/Turpentine/cat.turp
var c : int;
c := readByte();
while c >= 0 {
  printByte(c);
  c := readByte();
}
