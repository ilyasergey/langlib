Here are the tasks for today.

Part 1

* Move Langlib/Languages/Turpentine to become Langlib/Languages/Turpentine, update all dependencies, and all documentation consistently.
* I don't like the name Class.lean. Move all definitions related to computability and certified compilation to Common. One file should describe everything related to computability, ad the meta level, another should talk about correct compilation. 
* Correct compilation structures/classes should be generic for the source and language, not specific to Turpentine. Then in Turpentine/Compile folder they need to be instantiated for Turpentine and the specific language L
* Derived.lean should leave under Turpentine/Compile
* We need a richer definition of the cedtified compilation that includes IO: the current doesn't. In the Common/... file that trals about certified compilation, make another definition that defines compilation with IO, generic. That might require redefining the shape of all small-steps semantics we have (and a general shape in the theorems) that we have to account for the streams of input/output events (some don't emit any so the stream remains empty). The Certified compilation with IO thus accounts for these streams too, while the ordinary certified compilation doesn't. Currently all our theorems are proven for ordinary certified compilation. Prove that IOAware certified compilation implies the ordinary one, and fix all statements we have appropriately. Don't prove io-aware compilation just yet for those we can, we will do it later.
* Once you do all of these, update all documents, including readmes and certified-compilation.
* Stop and wait for commands once you reach this part.
* Commit and push regularly

===============================

Part 2

* Implement the TC proofs for all the remaining languages and give them derived compilers from Turpentine. Update all documentation, matrices, etc. Give plenty of examples.
* Implement bespoke compilers from turpentine for all TC languages.
* Implement a common test suite based on Turpentine, having 15-20 interesting programs, which compile to most of the languages and return expected results. Avoid those with IO, so we could run them automatically, and for all languages where we have the compilers. Each test program should be implemented first-hand in all of those languages, not simply as a result of compiling from Turpentine. 
* Once you have it, also replicate new programs back to the folders with examples in the languages, and document each example in the special section of the respective language specification.
* Document these tests in the readme, briefly, and in a more detailed way in the dedcicated folder.
* Stop and wait for commands once you reach this part.
* Commit and push regularly

===================

Part 3

* Refactor proofs for bespoke compilers for which we already have compiler correctness, to take IO into the account.
* Document all of that.

